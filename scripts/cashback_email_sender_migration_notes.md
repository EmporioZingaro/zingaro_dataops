# Cashback email sender: migration checklist

Use this checklist before promoting the cashback sender to production.

## Hardcoded variables that must be reviewed

- `SENDGRID_SECRET_PATH`
- `SENDGRID_TEMPLATE_ID`
- `FROM_EMAIL`
- `FROM_NAME`
- `CASHBACK_TABLE`
- `PEDIDOS_TABLE`
- `PEDIDOS_STORE_COLUMN`
- `TEST_MODE`
- `TEST_EMAIL`
- `EMAIL_SEND_LIMIT`

## BigQuery tables that must be aligned

- Cashback source table (`CASHBACK_TABLE`)
  - Must point to the canonical table generated by the quarter orchestrator (`...cashback`)
  - Required fields:
    - `quarter_id`
    - `quarter_start`
    - `quarter_end`
    - `cliente_nome`
    - `cliente_cpf`
    - `cliente_email`
    - `points`
    - `spend`
    - `tier`
    - `cashback`
- Pedidos source table (`PEDIDOS_TABLE`)
  - Required fields:
    - `pedido_dia`
    - `pedido_id`
    - `vendedor_nome`
    - `pedido_pontos`
    - `cliente_email`
    - `store_prefix` (or the configured `PEDIDOS_STORE_COLUMN`)

## Recipient email policy

- Use `cliente_email` exactly as delivered by the cashback table.
- The cashback table is the source of truth for email selection and conflict resolution.

## SendGrid variables

- Existing variables: `cliente_nome`, `cashback`, `tier`, `porcentagem_cashback`, `points`, `pedidos`
- Pedidos-level store variable: `store_location` (sent as raw `store_prefix` value from data)
- Quarter context variables available to template logic: `quarter_id`, `quarter_start`, `quarter_end`
